---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="Point Loma Moving Services" description="Professional moving services in Point Loma, San Diego - content coming soon." noindex={true}>

  <section class="location-hero">
    <div class="hero-background"></div>
    <div class="hero-overlay"></div>
    <div class="hero-content-wrapper">
      <div class="location-badge">La Jolla</div>
      <h1>[Placeholder Hero Title]</h1>
      <p>[Placeholder hero description text goes here]</p>
    </div>
  </section>

  <section class="location-content-section">
    <div class="container">
      <div class="content-layout">
        <div class="main-column">
          <!-- Intro Section -->
          <div class="intro-section">
            <h2>[Placeholder Section Heading]</h2>
            <p>[Placeholder section description text that introduces the content below]</p>
          </div>

          <!-- Content Cards - SEO content sections -->
          <div class="content-card">
            <h3>[Placeholder Content Card Title]</h3>
            <p>[Placeholder paragraph text for this content card. This is where you would add detailed information about La Jolla, the neighborhood, moving services, and other SEO-focused content. Add 100-150 words here about the area and services.]</p>
          </div>

          <div class="content-card">
            <h3>[Placeholder Second Card Title]</h3>
            <p>[Placeholder paragraph text for the second content card. Add more details about La Jolla moving services, local expertise, and neighborhood information here. Another 100-150 words of SEO content.]</p>
          </div>

          <div class="content-card">
            <h3>[Placeholder Third Card Title]</h3>
            <p>[Placeholder paragraph text for the third content card with additional details about the community, local moving challenges, and why Mighty Movers is the best choice for La Jolla residents. 100-150 words.]</p>
          </div>

          <!-- Services Links - Simple list style -->
          <div class="services-links-section">
            <h2>[Our Moving Services]</h2>
            <div class="services-links-list">
              <a href="/services/local-moving" class="service-link-item">
                <div class="service-link-content">
                  <h4>Local Moving</h4>
                  <p>Professional local moving services in La Jolla</p>
                </div>
                <span class="service-link-arrow">→</span>
              </a>
              <a href="/services" class="service-link-item">
                <div class="service-link-content">
                  <h4>[Service 2]</h4>
                  <p>[Service description]</p>
                </div>
                <span class="service-link-arrow">→</span>
              </a>
              <a href="/services" class="service-link-item">
                <div class="service-link-content">
                  <h4>[Service 3]</h4>
                  <p>[Service description]</p>
                </div>
                <span class="service-link-arrow">→</span>
              </a>
              <a href="/services" class="service-link-item">
                <div class="service-link-content">
                  <h4>[Service 4]</h4>
                  <p>[Service description]</p>
                </div>
                <span class="service-link-arrow">→</span>
              </a>
            </div>
          </div>

          <!-- Why Choose Us in La Jolla - Location specific -->
          <div class="why-section">
            <h2>[Why Choose Us in La Jolla]</h2>
            <div class="benefits-list">
              <div class="benefit-item">
                <div class="benefit-icon"></div>
                <div class="benefit-content">
                  <h4>[Benefit Title]</h4>
                  <p>[Placeholder benefit description]</p>
                </div>
              </div>
              <div class="benefit-item">
                <div class="benefit-icon"></div>
                <div class="benefit-content">
                  <h4>[Benefit Title]</h4>
                  <p>[Placeholder benefit description]</p>
                </div>
              </div>
              <div class="benefit-item">
                <div class="benefit-icon"></div>
                <div class="benefit-content">
                  <h4>[Benefit Title]</h4>
                  <p>[Placeholder benefit description]</p>
                </div>
              </div>
              <div class="benefit-item">
                <div class="benefit-icon"></div>
                <div class="benefit-content">
                  <h4>[Benefit Title]</h4>
                  <p>[Placeholder benefit description]</p>
                </div>
              </div>
            </div>
          </div>

          <!-- Other Neighborhoods - Moved lower on page -->
          <div class="neighborhoods-section">
            <h2>[Other Neighborhoods]</h2>
            <div class="neighborhood-grid">
              <div class="neighborhood-card">
                <div class="neighborhood-number">01</div>
                <h3>[Neighborhood 1]</h3>
                <p>[Placeholder description]</p>
              </div>
              <div class="neighborhood-card">
                <div class="neighborhood-number">02</div>
                <h3>[Neighborhood 2]</h3>
                <p>[Placeholder description]</p>
              </div>
              <div class="neighborhood-card">
                <div class="neighborhood-number">03</div>
                <h3>[Neighborhood 3]</h3>
                <p>[Placeholder description]</p>
              </div>
              <div class="neighborhood-card">
                <div class="neighborhood-number">04</div>
                <h3>[Neighborhood 4]</h3>
                <p>[Placeholder description]</p>
              </div>
            </div>
          </div>

          <!-- FAQ Section - Matching service pages -->
          <div class="faq-section">
            <h2>[Frequently Asked Questions]</h2>
            <div class="faq-list">
              <div class="faq-item">
                <button class="faq-question">
                  <h4>[Placeholder FAQ Question 1?]</h4>
                  <span class="faq-icon">+</span>
                </button>
                <div class="faq-answer">
                  <p>[Placeholder answer to the first frequently asked question goes here with detailed information.]</p>
                </div>
              </div>
              <div class="faq-item">
                <button class="faq-question">
                  <h4>[Placeholder FAQ Question 2?]</h4>
                  <span class="faq-icon">+</span>
                </button>
                <div class="faq-answer">
                  <p>[Placeholder answer to the second frequently asked question with helpful details.]</p>
                </div>
              </div>
              <div class="faq-item">
                <button class="faq-question">
                  <h4>[Placeholder FAQ Question 3?]</h4>
                  <span class="faq-icon">+</span>
                </button>
                <div class="faq-answer">
                  <p>[Placeholder answer to the third frequently asked question explaining the topic.]</p>
                </div>
              </div>
              <div class="faq-item">
                <button class="faq-question">
                  <h4>[Placeholder FAQ Question 4?]</h4>
                  <span class="faq-icon">+</span>
                </button>
                <div class="faq-answer">
                  <p>[Placeholder answer to the fourth frequently asked question with more information.]</p>
                </div>
              </div>
              <div class="faq-item">
                <button class="faq-question">
                  <h4>[Placeholder FAQ Question 5?]</h4>
                  <span class="faq-icon">+</span>
                </button>
                <div class="faq-answer">
                  <p>[Placeholder answer to the fifth frequently asked question providing clarity.]</p>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Sidebar - Same CTA buttons as service pages -->
        <aside class="sidebar-column">
          <div class="cta-card">
            <h3>[CTA Card Title]</h3>
            <p>[Placeholder call-to-action description text]</p>
            <a href="/contact" class="btn btn-primary">[Button Text]</a>
          </div>

          <div class="cta-card cta-dark">
            <h3>[Second CTA Title]</h3>
            <p>[Placeholder text for second call-to-action]</p>
            <a href="tel:6195556683" class="btn btn-secondary">[Call Button]</a>
          </div>

          <div class="info-card">
            <h4>[Info Card Heading]</h4>
            <ul>
              <li>[Placeholder list item 1]</li>
              <li>[Placeholder list item 2]</li>
              <li>[Placeholder list item 3]</li>
              <li>[Placeholder list item 4]</li>
            </ul>
          </div>

          <div class="info-card">
            <h4>[Additional Info]</h4>
            <p>[Placeholder text for additional information card]</p>
          </div>
        </aside>
      </div>
    </div>
  </section>

  <!-- CTA Section - Matching service pages exactly -->
  <section class="cta-section">
    <div class="cta-content">
      <h2>[Placeholder CTA Heading]</h2>
      <p>[Placeholder CTA description text to encourage action]</p>
      <div class="cta-buttons">
        <a href="/contact" class="btn btn-primary btn-large">[Primary CTA Button]</a>
        <a href="tel:6195556683" class="btn btn-secondary btn-large">[Secondary CTA Button]</a>
      </div>
    </div>
  </section>
</Layout>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const faqItems = document.querySelectorAll('.faq-item');

    faqItems.forEach(item => {
      const question = item.querySelector('.faq-question');
      const answer = item.querySelector('.faq-answer');
      const icon = item.querySelector('.faq-icon');

      question?.addEventListener('click', () => {
        const isOpen = item.classList.contains('active');

        faqItems.forEach(otherItem => {
          if (otherItem !== item) {
            otherItem.classList.remove('active');
            const otherAnswer = otherItem.querySelector('.faq-answer') as HTMLElement;
            const otherIcon = otherItem.querySelector('.faq-icon');
            if (otherAnswer) otherAnswer.style.maxHeight = '0';
            if (otherIcon) otherIcon.textContent = '+';
          }
        });

        if (isOpen) {
          item.classList.remove('active');
          if (answer) (answer as HTMLElement).style.maxHeight = '0';
          if (icon) icon.textContent = '+';
        } else {
          item.classList.add('active');
          if (answer) (answer as HTMLElement).style.maxHeight = answer.scrollHeight + 'px';
          if (icon) icon.textContent = '−';
        }
      });
    });
  });
</script>

<style>
  /* Hero - Left-aligned, location-focused */
  .location-hero {
    min-height: 55vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
    padding: 5rem 0;
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('https://images.unsplash.com/photo-1581368087049-e82c8f0e2a6f?w=1920&q=80') center/cover no-repeat;
    z-index: 0;
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(90deg, rgba(0, 0, 0, 0.9) 0%, rgba(0, 0, 0, 0.6) 60%, transparent 100%);
    z-index: 1;
  }

  .hero-content-wrapper {
    position: relative;
    z-index: 2;
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 3rem;
    max-width: 650px;
  }

  .location-badge {
    display: inline-block;
    background: var(--yellow);
    color: var(--black);
    padding: 0.75rem 2rem;
    font-size: 0.9rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 2rem;
    clip-path: polygon(0 0, calc(100% - 12px) 0, 100% 12px, 100% 100%, 0 100%);
  }

  .location-hero h1 {
    font-size: clamp(3.5rem, 7vw, 5.5rem);
    font-weight: 900;
    color: var(--white);
    margin-bottom: 2rem;
    letter-spacing: -3px;
    line-height: 0.95;
    min-height: 5.5rem;
  }

  .location-hero p {
    font-size: 1.3rem;
    color: var(--gray-200);
    line-height: 1.7;
    max-width: 550px;
    min-height: 2rem;
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 3rem;
  }

  .location-content-section {
    padding: 6rem 0;
    position: relative;
    overflow: hidden;
  }

  .location-content-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image:
      linear-gradient(45deg, transparent 45%, rgba(246, 245, 16, 0.02) 45%, rgba(246, 245, 16, 0.02) 55%, transparent 55%),
      linear-gradient(-45deg, transparent 45%, rgba(0, 0, 0, 0.02) 45%, rgba(0, 0, 0, 0.02) 55%, transparent 55%);
    background-size: 60px 60px;
    opacity: 0.5;
    pointer-events: none;
  }

  .content-layout {
    display: grid;
    grid-template-columns: 1fr 400px;
    gap: 4rem;
  }

  .main-column {
    min-width: 0;
  }

  /* Shared heading styles with service pages */
  .intro-section,
  .neighborhoods-section,
  .why-section,
  .services-overview,
  .faq-section {
    margin-bottom: 5rem;
  }

  .intro-section h2,
  .neighborhoods-section h2,
  .why-section h2,
  .services-overview h2,
  .faq-section h2 {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 900;
    color: var(--black);
    margin-bottom: 3rem;
    letter-spacing: -1px;
    min-height: 3.5rem;
  }

  .intro-section p {
    font-size: 1.2rem;
    color: var(--gray-600);
    line-height: 1.8;
    min-height: 2rem;
  }

  /* Content Cards - SEO content sections */
  .content-card {
    background: var(--white);
    padding: 2.5rem 2rem;
    border-radius: 0;
    border: 3px solid var(--black);
    margin-bottom: 2.5rem;
    transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
    position: relative;
    clip-path: polygon(0 0, calc(100% - 20px) 0, 100% 20px, 100% 100%, 0 100%);
  }

  .content-card::before {
    content: '';
    position: absolute;
    top: 0;
    right: 0;
    width: 20px;
    height: 20px;
    background: var(--yellow);
    clip-path: polygon(100% 0, 100% 100%, 0 0);
  }

  .content-card:hover {
    border-color: var(--yellow);
    transform: translate(-4px, -4px);
    box-shadow: 8px 8px 0 rgba(246, 245, 16, 0.9), 0 0 30px rgba(246, 245, 16, 0.4);
  }

  .content-card h3 {
    font-size: 1.8rem;
    font-weight: 800;
    color: var(--black);
    margin-bottom: 1.25rem;
    min-height: 1.8rem;
  }

  .content-card p {
    font-size: 1.05rem;
    color: var(--gray-600);
    line-height: 1.8;
    min-height: 1.5rem;
  }

  /* Neighborhoods Grid - Location-specific design */
  .neighborhood-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
  }

  .neighborhood-card {
    background: var(--gray-50);
    padding: 2.5rem 2rem;
    border-left: 5px solid var(--yellow);
    transition: all 0.3s ease;
    position: relative;
  }

  .neighborhood-card:hover {
    background: var(--white);
    border-left-width: 8px;
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
    transform: translateX(8px);
  }

  .neighborhood-number {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    background: var(--black);
    color: var(--yellow);
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 900;
    font-size: 0.9rem;
  }

  .neighborhood-card h3 {
    font-size: 1.4rem;
    font-weight: 800;
    color: var(--black);
    margin-bottom: 0.75rem;
    min-height: 1.4rem;
  }

  .neighborhood-card p {
    font-size: 1rem;
    color: var(--gray-600);
    line-height: 1.7;
    min-height: 1rem;
  }

  /* Why Choose Us - List style instead of cards */
  .benefits-list {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .benefit-item {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 2rem;
    padding: 2rem;
    background: var(--white);
    border: 2px solid var(--gray-200);
    border-left: 5px solid var(--black);
    transition: all 0.3s ease;
  }

  .benefit-item:hover {
    border-left-color: var(--yellow);
    border-left-width: 8px;
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
    transform: translateX(8px);
  }

  .benefit-icon {
    width: 3.5rem;
    height: 3.5rem;
    background: var(--yellow);
    clip-path: polygon(0 0, calc(100% - 10px) 0, 100% 10px, 100% 100%, 0 100%);
    flex-shrink: 0;
  }

  .benefit-content h4 {
    font-size: 1.3rem;
    font-weight: 800;
    color: var(--black);
    margin-bottom: 0.75rem;
    min-height: 1.3rem;
  }

  .benefit-content p {
    font-size: 1.05rem;
    color: var(--gray-600);
    line-height: 1.7;
    min-height: 1rem;
  }

  /* Services Links - Clean list style */
  .services-links-section {
    margin-bottom: 5rem;
    background: var(--gray-50);
    padding: 3rem 2.5rem;
    border-left: 5px solid var(--yellow);
  }

  .services-links-list {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .service-link-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 2rem 0;
    border-bottom: 2px solid var(--gray-200);
    text-decoration: none;
    color: var(--black);
    transition: all 0.3s ease;
    position: relative;
  }

  .service-link-item:last-child {
    border-bottom: none;
  }

  .service-link-item:hover {
    padding-left: 1.5rem;
    border-bottom-color: var(--yellow);
  }

  .service-link-item:hover::before {
    content: '';
    position: absolute;
    left: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 5px;
    height: 60%;
    background: var(--yellow);
  }

  .service-link-content h4 {
    font-size: 1.4rem;
    font-weight: 800;
    color: var(--black);
    margin-bottom: 0.5rem;
    min-height: 1.4rem;
  }

  .service-link-content p {
    font-size: 1rem;
    color: var(--gray-600);
    line-height: 1.6;
    min-height: 1rem;
  }

  .service-link-arrow {
    font-size: 2rem;
    color: var(--black);
    font-weight: 300;
    transition: all 0.3s ease;
    flex-shrink: 0;
    margin-left: 2rem;
  }

  .service-link-item:hover .service-link-arrow {
    color: var(--yellow);
    transform: translateX(10px);
  }

  /* FAQ Section - Exact match to service pages */
  .faq-section {
    padding-top: 4rem;
    border-top: 3px solid var(--gray-200);
  }

  .faq-list {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .faq-item {
    background: var(--white);
    border: 3px solid var(--black);
    border-radius: 0;
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
    position: relative;
    clip-path: polygon(0 0, calc(100% - 20px) 0, 100% 20px, 100% 100%, 0 100%);
  }

  .faq-item::before {
    content: '';
    position: absolute;
    top: 0;
    right: 0;
    width: 20px;
    height: 20px;
    background: var(--yellow);
    clip-path: polygon(100% 0, 100% 100%, 0 0);
  }

  .faq-item:hover {
    border-color: var(--yellow);
    transform: translate(-4px, -4px);
    box-shadow: 8px 8px 0 rgba(246, 245, 16, 0.9), 0 0 30px rgba(246, 245, 16, 0.4);
  }

  .faq-question {
    width: 100%;
    padding: 2rem;
    background: none;
    border: none;
    cursor: pointer;
    display: flex;
    justify-content: space-between;
    align-items: center;
    text-align: left;
    transition: all 0.3s ease;
  }

  .faq-question:hover {
    background: rgba(246, 245, 16, 0.05);
  }

  .faq-question h4 {
    font-size: 1.3rem;
    font-weight: 800;
    color: var(--black);
    margin: 0;
    min-height: 1.3rem;
  }

  .faq-icon {
    font-size: 2rem;
    font-weight: 300;
    color: var(--yellow);
    line-height: 1;
    transition: transform 0.3s ease;
    flex-shrink: 0;
    margin-left: 1rem;
    width: 2rem;
    text-align: center;
  }

  .faq-item.active .faq-icon {
    transform: rotate(45deg);
  }

  .faq-answer {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
  }

  .faq-answer p {
    font-size: 1.1rem;
    color: var(--gray-600);
    line-height: 1.8;
    padding: 0 2rem 2rem;
    min-height: 1.5rem;
  }

  /* Sidebar - Matching service pages exactly */
  .sidebar-column {
    position: sticky;
    top: 6rem;
    height: fit-content;
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .cta-card {
    background: var(--black);
    color: var(--white);
    padding: 2.5rem 2rem;
    border-radius: 0;
    transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
    position: relative;
    border: 3px solid var(--gray-800);
    overflow: hidden;
  }

  .cta-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 5px;
    background: var(--yellow);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.4s ease;
  }

  .cta-card:hover {
    border-color: var(--yellow);
    box-shadow: 0 8px 30px rgba(246, 245, 16, 0.3);
  }

  .cta-card:hover::before {
    transform: scaleX(1);
  }

  .cta-dark {
    background: var(--black);
    color: var(--white);
  }

  .cta-card h3 {
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--white);
    margin-bottom: 1rem;
    min-height: 1.5rem;
  }

  .cta-card p {
    color: var(--gray-400);
    margin-bottom: 1.5rem;
    line-height: 1.6;
    min-height: 1.5rem;
  }

  .info-card {
    background: var(--white);
    padding: 2rem;
    border: 3px solid var(--black);
    border-radius: 0;
    transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
    position: relative;
    clip-path: polygon(0 0, calc(100% - 15px) 0, 100% 15px, 100% 100%, 0 100%);
  }

  .info-card::before {
    content: '';
    position: absolute;
    top: 0;
    right: 0;
    width: 15px;
    height: 15px;
    background: var(--yellow);
    clip-path: polygon(100% 0, 100% 100%, 0 0);
  }

  .info-card:hover {
    border-color: var(--yellow);
    transform: translate(-4px, -4px);
    box-shadow: 8px 8px 0 rgba(246, 245, 16, 0.9), 0 0 30px rgba(246, 245, 16, 0.4);
  }

  .info-card h4 {
    font-size: 1.2rem;
    font-weight: 800;
    color: var(--black);
    margin-bottom: 1.5rem;
    min-height: 1.2rem;
  }

  .info-card ul {
    list-style: none;
    padding: 0;
  }

  .info-card li {
    padding: 0.75rem 0;
    color: var(--gray-600);
    font-size: 0.95rem;
    border-bottom: 1px solid var(--gray-200);
    min-height: 1rem;
  }

  .info-card li:last-child {
    border-bottom: none;
  }

  .info-card p {
    color: var(--gray-600);
    line-height: 1.6;
    font-size: 0.95rem;
    min-height: 1.5rem;
  }

  /* Buttons - Matching service pages exactly */
  .btn {
    display: inline-block;
    width: 100%;
    padding: 1rem 2rem;
    font-size: 1rem;
    font-weight: 700;
    text-decoration: none;
    text-align: center;
    border-radius: 0;
    transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
    border: 2px solid transparent;
    min-height: 3rem;
    clip-path: polygon(12px 0, 100% 0, calc(100% - 12px) 100%, 0 100%);
  }

  .btn-primary {
    background: var(--yellow);
    color: var(--black);
    border-color: var(--yellow);
    box-shadow: 4px 4px 0 rgba(0, 0, 0, 0.2);
  }

  .btn-primary:hover {
    background: var(--white);
    color: var(--black);
    border-color: var(--yellow);
    transform: translate(-2px, -2px);
    box-shadow: 6px 6px 0 rgba(246, 245, 16, 0.7), 0 0 25px rgba(246, 245, 16, 0.5);
  }

  .cta-dark .btn-primary {
    background: var(--yellow);
    color: var(--black);
  }

  .btn-secondary {
    background: transparent;
    color: var(--white);
    border-color: var(--white);
    box-shadow: 4px 4px 0 rgba(255, 255, 255, 0.1);
  }

  .btn-secondary:hover {
    background: var(--white);
    color: var(--black);
    transform: translate(-2px, -2px);
    box-shadow: 6px 6px 0 rgba(255, 255, 255, 0.3);
  }

  .btn-large {
    padding: 1.5rem 3rem;
    font-size: 1.1rem;
  }

  /* CTA Section - Exact match to service pages */
  .cta-section {
    background: linear-gradient(135deg, #000000 0%, #1a1a1a 100%);
    padding: 6rem 2rem;
    text-align: center;
    position: relative;
    overflow: hidden;
    margin-top: 4rem;
  }

  .cta-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 50% 50%, rgba(246, 245, 16, 0.1) 0%, transparent 60%);
  }

  .cta-content {
    position: relative;
    z-index: 1;
    max-width: 900px;
    margin: 0 auto;
  }

  .cta-section h2 {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 900;
    color: var(--white);
    margin-bottom: 1rem;
    letter-spacing: -1px;
    min-height: 2.5rem;
  }

  .cta-section p {
    font-size: 1.3rem;
    color: var(--gray-300);
    margin-bottom: 3rem;
    min-height: 1.5rem;
  }

  .cta-buttons {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .cta-section .btn-primary {
    background: var(--yellow);
    color: var(--black);
    border-color: var(--yellow);
  }

  .cta-section .btn-primary:hover {
    background: transparent;
    color: var(--yellow);
    border-color: var(--yellow);
    box-shadow: 6px 6px 0 rgba(246, 245, 16, 0.7), 0 0 25px rgba(246, 245, 16, 0.5);
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .content-layout {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .sidebar-column {
      position: relative;
      top: 0;
    }

    .neighborhood-grid {
      grid-template-columns: 1fr;
    }

    .services-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .container {
      padding: 0 1.5rem;
    }

    .hero-content-wrapper {
      padding: 0 1.5rem;
    }

    .location-content-section {
      padding: 3rem 0;
    }

    .services-links-section {
      padding: 2rem 1.5rem;
    }

    .service-link-item {
      flex-direction: column;
      align-items: flex-start;
      padding: 1.5rem 0;
    }

    .service-link-arrow {
      align-self: flex-end;
      margin-left: 0;
      margin-top: 1rem;
    }

    .benefit-item {
      grid-template-columns: 1fr;
      gap: 1rem;
    }
  }
</style>
