---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="La Jolla Moving Services" description="Professional moving services in La Jolla, San Diego. Local movers serving La Jolla Shores, La Jolla Village, and surrounding coastal areas.">
  <section class="location-hero">
    <div class="hero-background"></div>
    <div class="hero-overlay"></div>
    <div class="hero-content-wrapper">
      <div class="location-badge">La Jolla</div>
      <h1>[Placeholder Hero Title]</h1>
      <p>[Placeholder hero description text goes here]</p>
    </div>
  </section>

  <section class="location-content-section">
    <div class="container">
      <div class="content-layout">
        <div class="main-column">
          <div class="intro-section">
            <h2>[Placeholder Section Heading]</h2>
            <p>[Placeholder section description text that introduces the content below]</p>
          </div>

          <div class="location-card">
            <h3>[Placeholder Content Card Title]</h3>
            <p>[Placeholder paragraph text for this content card. This is where you would add detailed information about this topic.]</p>
          </div>

          <div class="location-card">
            <h3>[Placeholder Second Card Title]</h3>
            <p>[Placeholder paragraph text for the second content card. Add more details and information here.]</p>
          </div>

          <div class="features-grid">
            <div class="feature-box">
              <div class="feature-icon"></div>
              <h4>[Feature 1 Title]</h4>
              <p>[Placeholder description for feature one]</p>
            </div>
            <div class="feature-box">
              <div class="feature-icon"></div>
              <h4>[Feature 2 Title]</h4>
              <p>[Placeholder description for feature two]</p>
            </div>
            <div class="feature-box">
              <div class="feature-icon"></div>
              <h4>[Feature 3 Title]</h4>
              <p>[Placeholder description for feature three]</p>
            </div>
            <div class="feature-box">
              <div class="feature-icon"></div>
              <h4>[Feature 4 Title]</h4>
              <p>[Placeholder description for feature four]</p>
            </div>
          </div>

          <div class="neighborhoods-section">
            <h2>[Placeholder Neighborhoods Heading]</h2>
            <div class="neighborhood-grid">
              <div class="neighborhood-item">
                <h4>[Neighborhood 1]</h4>
                <p>[Placeholder description]</p>
              </div>
              <div class="neighborhood-item">
                <h4>[Neighborhood 2]</h4>
                <p>[Placeholder description]</p>
              </div>
              <div class="neighborhood-item">
                <h4>[Neighborhood 3]</h4>
                <p>[Placeholder description]</p>
              </div>
              <div class="neighborhood-item">
                <h4>[Neighborhood 4]</h4>
                <p>[Placeholder description]</p>
              </div>
            </div>
          </div>

          <div class="faq-section">
            <h2>[Frequently Asked Questions]</h2>
            <div class="faq-list">
              <div class="faq-item">
                <button class="faq-question">
                  <h4>[Placeholder FAQ Question 1?]</h4>
                  <span class="faq-icon">+</span>
                </button>
                <div class="faq-answer">
                  <p>[Placeholder answer to the first frequently asked question goes here with detailed information.]</p>
                </div>
              </div>
              <div class="faq-item">
                <button class="faq-question">
                  <h4>[Placeholder FAQ Question 2?]</h4>
                  <span class="faq-icon">+</span>
                </button>
                <div class="faq-answer">
                  <p>[Placeholder answer to the second frequently asked question with helpful details.]</p>
                </div>
              </div>
              <div class="faq-item">
                <button class="faq-question">
                  <h4>[Placeholder FAQ Question 3?]</h4>
                  <span class="faq-icon">+</span>
                </button>
                <div class="faq-answer">
                  <p>[Placeholder answer to the third frequently asked question explaining the topic.]</p>
                </div>
              </div>
              <div class="faq-item">
                <button class="faq-question">
                  <h4>[Placeholder FAQ Question 4?]</h4>
                  <span class="faq-icon">+</span>
                </button>
                <div class="faq-answer">
                  <p>[Placeholder answer to the fourth frequently asked question with more information.]</p>
                </div>
              </div>
              <div class="faq-item">
                <button class="faq-question">
                  <h4>[Placeholder FAQ Question 5?]</h4>
                  <span class="faq-icon">+</span>
                </button>
                <div class="faq-answer">
                  <p>[Placeholder answer to the fifth frequently asked question providing clarity.]</p>
                </div>
              </div>
            </div>
          </div>
        </div>

        <aside class="sidebar-column">
          <div class="cta-box">
            <h3>[CTA Card Title]</h3>
            <p>[Placeholder call-to-action description text]</p>
            <a href="/contact" class="btn btn-primary">[Button Text]</a>
          </div>

          <div class="cta-box cta-dark">
            <h3>[Second CTA Title]</h3>
            <p>[Placeholder text for second call-to-action]</p>
            <a href="tel:6195556683" class="btn btn-secondary">[Call Button]</a>
          </div>

          <div class="info-box">
            <h4>[Info Card Heading]</h4>
            <ul>
              <li>[Placeholder list item 1]</li>
              <li>[Placeholder list item 2]</li>
              <li>[Placeholder list item 3]</li>
              <li>[Placeholder list item 4]</li>
            </ul>
          </div>

          <div class="info-box">
            <h4>[Additional Info]</h4>
            <p>[Placeholder text for additional information card]</p>
          </div>
        </aside>
      </div>
    </div>
  </section>

  <section class="cta-section">
    <div class="cta-content">
      <h2>[Placeholder CTA Heading]</h2>
      <p>[Placeholder CTA description text to encourage action]</p>
      <div class="cta-buttons">
        <a href="/contact" class="btn btn-primary btn-large">[Primary CTA Button]</a>
        <a href="tel:6195556683" class="btn btn-secondary btn-large">[Secondary CTA Button]</a>
      </div>
    </div>
  </section>
</Layout>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const faqItems = document.querySelectorAll('.faq-item');

    faqItems.forEach(item => {
      const question = item.querySelector('.faq-question');
      const answer = item.querySelector('.faq-answer');
      const icon = item.querySelector('.faq-icon');

      question?.addEventListener('click', () => {
        const isOpen = item.classList.contains('active');

        faqItems.forEach(otherItem => {
          if (otherItem !== item) {
            otherItem.classList.remove('active');
            const otherAnswer = otherItem.querySelector('.faq-answer') as HTMLElement;
            const otherIcon = otherItem.querySelector('.faq-icon');
            if (otherAnswer) otherAnswer.style.maxHeight = '0';
            if (otherIcon) otherIcon.textContent = '+';
          }
        });

        if (isOpen) {
          item.classList.remove('active');
          if (answer) (answer as HTMLElement).style.maxHeight = '0';
          if (icon) icon.textContent = '+';
        } else {
          item.classList.add('active');
          if (answer) (answer as HTMLElement).style.maxHeight = answer.scrollHeight + 'px';
          if (icon) icon.textContent = '−';
        }
      });
    });
  });
</script>

<style>
  /* Hero Section - Left-aligned like service pages but with location distinction */
  .location-hero {
    min-height: 55vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
    padding: 5rem 0;
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('https://images.unsplash.com/photo-1581368087049-e82c8f0e2a6f?w=1920&q=80') center/cover no-repeat;
    z-index: 0;
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(90deg, rgba(0, 0, 0, 0.9) 0%, rgba(0, 0, 0, 0.6) 60%, transparent 100%);
    z-index: 1;
  }

  .hero-content-wrapper {
    position: relative;
    z-index: 2;
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 3rem;
    max-width: 650px;
  }

  .location-badge {
    display: inline-block;
    background: var(--yellow);
    color: var(--black);
    padding: 0.75rem 2rem;
    font-size: 0.9rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 2rem;
    clip-path: polygon(0 0, calc(100% - 12px) 0, 100% 12px, 100% 100%, 0 100%);
  }

  .location-hero h1 {
    font-size: clamp(3.5rem, 7vw, 5.5rem);
    font-weight: 900;
    color: var(--white);
    margin-bottom: 2rem;
    letter-spacing: -3px;
    line-height: 0.95;
    min-height: 5.5rem;
  }

  .location-hero p {
    font-size: 1.3rem;
    color: var(--gray-200);
    line-height: 1.7;
    max-width: 550px;
    min-height: 2rem;
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 3rem;
  }

  .location-content-section {
    padding: 6rem 0;
    position: relative;
    overflow: hidden;
  }

  .location-content-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image:
      linear-gradient(45deg, transparent 45%, rgba(246, 245, 16, 0.02) 45%, rgba(246, 245, 16, 0.02) 55%, transparent 55%),
      linear-gradient(-45deg, transparent 45%, rgba(0, 0, 0, 0.02) 45%, rgba(0, 0, 0, 0.02) 55%, transparent 55%);
    background-size: 60px 60px;
    opacity: 0.5;
    pointer-events: none;
  }

  .content-layout {
    display: grid;
    grid-template-columns: 1fr 400px;
    gap: 4rem;
  }

  .main-column {
    min-width: 0;
  }

  /* Intro Section */
  .intro-section {
    margin-bottom: 4rem;
  }

  .intro-section h2 {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 900;
    color: var(--black);
    margin-bottom: 1.5rem;
    letter-spacing: -1px;
    min-height: 3.5rem;
  }

  .intro-section p {
    font-size: 1.2rem;
    color: var(--gray-600);
    line-height: 1.8;
    min-height: 2rem;
  }

  /* Location Cards - Similar to service content cards */
  .location-card {
    background: var(--white);
    padding: 3rem;
    margin-bottom: 3rem;
    border: 3px solid var(--black);
    border-radius: 0;
    transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
    position: relative;
    clip-path: polygon(0 0, calc(100% - 25px) 0, 100% 25px, 100% 100%, 0 100%);
  }

  .location-card::before {
    content: '';
    position: absolute;
    top: 0;
    right: 0;
    width: 25px;
    height: 25px;
    background: var(--yellow);
    clip-path: polygon(100% 0, 100% 100%, 0 0);
  }

  .location-card:hover {
    border-color: var(--yellow);
    transform: translate(-4px, -4px);
    box-shadow: 8px 8px 0 rgba(246, 245, 16, 0.9), 0 0 30px rgba(246, 245, 16, 0.4);
  }

  .location-card h3 {
    font-size: 2rem;
    font-weight: 900;
    color: var(--black);
    margin-bottom: 1.5rem;
    letter-spacing: -1px;
    min-height: 2rem;
  }

  .location-card p {
    font-size: 1.1rem;
    color: var(--gray-600);
    line-height: 1.8;
    min-height: 1.5rem;
  }

  /* Features Grid - Similar style to service pages */
  .features-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    margin-bottom: 4rem;
  }

  .feature-box {
    background: var(--black);
    color: var(--white);
    padding: 2.5rem 2rem;
    border-radius: 0;
    transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
    border: 3px solid var(--gray-800);
    position: relative;
    overflow: hidden;
  }

  .feature-box::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 5px;
    height: 100%;
    background: var(--yellow);
    transform: scaleY(0);
    transform-origin: top;
    transition: transform 0.4s ease;
  }

  .feature-box:hover {
    border-color: var(--yellow);
    transform: translateX(8px);
    box-shadow: -8px 8px 0 rgba(246, 245, 16, 0.9), 0 0 30px rgba(246, 245, 16, 0.3);
  }

  .feature-box:hover::before {
    transform: scaleY(1);
  }

  .feature-icon {
    width: 4rem;
    height: 4rem;
    background: var(--yellow);
    border-radius: 0;
    margin-bottom: 1.5rem;
    clip-path: polygon(0 0, calc(100% - 12px) 0, 100% 12px, 100% 100%, 0 100%);
  }

  .feature-box h4 {
    font-size: 1.3rem;
    font-weight: 800;
    margin-bottom: 1rem;
    color: var(--white);
    min-height: 1.3rem;
  }

  .feature-box p {
    color: var(--gray-400);
    line-height: 1.8;
    font-size: 1rem;
    min-height: 1.5rem;
  }

  /* Neighborhoods Section - Distinct for location pages but polished */
  .neighborhoods-section {
    margin-bottom: 4rem;
    padding: 4rem 3rem;
    background: var(--black);
    color: var(--white);
    clip-path: polygon(0 0, calc(100% - 30px) 0, 100% 30px, 100% 100%, 0 100%);
    position: relative;
  }

  .neighborhoods-section::before {
    content: '';
    position: absolute;
    top: 0;
    right: 0;
    width: 30px;
    height: 30px;
    background: var(--yellow);
    clip-path: polygon(100% 0, 100% 100%, 0 0);
  }

  .neighborhoods-section h2 {
    font-size: clamp(2rem, 4vw, 2.8rem);
    font-weight: 900;
    color: var(--yellow);
    margin-bottom: 3rem;
    letter-spacing: -1px;
    min-height: 2.8rem;
  }

  .neighborhood-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2.5rem;
  }

  .neighborhood-item {
    border-left: 4px solid var(--yellow);
    padding-left: 1.5rem;
    transition: all 0.3s ease;
  }

  .neighborhood-item:hover {
    border-left-color: var(--white);
    transform: translateX(8px);
  }

  .neighborhood-item h4 {
    font-size: 1.3rem;
    font-weight: 800;
    color: var(--white);
    margin-bottom: 0.75rem;
    min-height: 1.3rem;
  }

  .neighborhood-item p {
    font-size: 1rem;
    color: var(--gray-400);
    line-height: 1.6;
    min-height: 1rem;
  }

  /* FAQ Section - Matching service page quality */
  .faq-section {
    margin-top: 5rem;
    padding-top: 4rem;
    border-top: 3px solid var(--gray-200);
  }

  .faq-section h2 {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 900;
    color: var(--black);
    margin-bottom: 3rem;
    letter-spacing: -1px;
    min-height: 3.5rem;
  }

  .faq-list {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .faq-item {
    background: var(--white);
    border: 3px solid var(--black);
    border-radius: 0;
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
    position: relative;
    clip-path: polygon(0 0, calc(100% - 20px) 0, 100% 20px, 100% 100%, 0 100%);
  }

  .faq-item::before {
    content: '';
    position: absolute;
    top: 0;
    right: 0;
    width: 20px;
    height: 20px;
    background: var(--yellow);
    clip-path: polygon(100% 0, 100% 100%, 0 0);
  }

  .faq-item:hover {
    border-color: var(--yellow);
    transform: translate(-4px, -4px);
    box-shadow: 8px 8px 0 rgba(246, 245, 16, 0.9), 0 0 30px rgba(246, 245, 16, 0.4);
  }

  .faq-question {
    width: 100%;
    padding: 2rem;
    background: none;
    border: none;
    cursor: pointer;
    display: flex;
    justify-content: space-between;
    align-items: center;
    text-align: left;
    transition: all 0.3s ease;
  }

  .faq-question:hover {
    background: rgba(246, 245, 16, 0.05);
  }

  .faq-question h4 {
    font-size: 1.3rem;
    font-weight: 800;
    color: var(--black);
    margin: 0;
    min-height: 1.3rem;
  }

  .faq-icon {
    font-size: 2rem;
    font-weight: 300;
    color: var(--yellow);
    line-height: 1;
    transition: transform 0.3s ease;
    flex-shrink: 0;
    margin-left: 1rem;
    width: 2rem;
    text-align: center;
  }

  .faq-item.active .faq-icon {
    transform: rotate(45deg);
  }

  .faq-answer {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
  }

  .faq-answer p {
    font-size: 1.1rem;
    color: var(--gray-600);
    line-height: 1.8;
    padding: 0 2rem 2rem;
    min-height: 1.5rem;
  }

  /* Sidebar - Matching service page quality with distinction */
  .sidebar-column {
    position: sticky;
    top: 6rem;
    height: fit-content;
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .cta-box {
    background: var(--yellow);
    color: var(--black);
    padding: 2.5rem 2rem;
    border-radius: 0;
    transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
    position: relative;
    clip-path: polygon(0 0, calc(100% - 20px) 0, 100% 20px, 100% 100%, 0 100%);
    border: 3px solid var(--black);
  }

  .cta-box::after {
    content: '';
    position: absolute;
    top: 0;
    right: 0;
    width: 20px;
    height: 20px;
    background: var(--black);
    clip-path: polygon(100% 0, 100% 100%, 0 0);
  }

  .cta-box:hover {
    transform: translate(-4px, -4px);
    box-shadow: 8px 8px 0 rgba(0, 0, 0, 0.3);
  }

  .cta-dark {
    background: var(--black);
    color: var(--white);
    border-color: var(--yellow);
  }

  .cta-dark::after {
    background: var(--yellow);
  }

  .cta-dark:hover {
    box-shadow: 8px 8px 0 rgba(246, 245, 16, 0.9), 0 0 30px rgba(246, 245, 16, 0.4);
  }

  .cta-box h3 {
    font-size: 1.5rem;
    font-weight: 900;
    margin-bottom: 1rem;
    min-height: 1.5rem;
  }

  .cta-box p {
    margin-bottom: 1.5rem;
    line-height: 1.6;
    min-height: 1.5rem;
  }

  .info-box {
    background: var(--white);
    padding: 2rem;
    border: 3px solid var(--black);
    border-radius: 0;
    transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
    position: relative;
    clip-path: polygon(0 0, calc(100% - 15px) 0, 100% 15px, 100% 100%, 0 100%);
  }

  .info-box::before {
    content: '';
    position: absolute;
    top: 0;
    right: 0;
    width: 15px;
    height: 15px;
    background: var(--yellow);
    clip-path: polygon(100% 0, 100% 100%, 0 0);
  }

  .info-box:hover {
    border-color: var(--yellow);
    transform: translate(-4px, -4px);
    box-shadow: 8px 8px 0 rgba(246, 245, 16, 0.9), 0 0 30px rgba(246, 245, 16, 0.4);
  }

  .info-box h4 {
    font-size: 1.2rem;
    font-weight: 800;
    color: var(--black);
    margin-bottom: 1.5rem;
    min-height: 1.2rem;
  }

  .info-box ul {
    list-style: none;
    padding: 0;
  }

  .info-box li {
    padding: 0.75rem 0;
    color: var(--gray-600);
    font-size: 0.95rem;
    border-bottom: 1px solid var(--gray-200);
    min-height: 1rem;
  }

  .info-box li:last-child {
    border-bottom: none;
  }

  .info-box p {
    color: var(--gray-600);
    line-height: 1.6;
    font-size: 0.95rem;
    min-height: 1.5rem;
  }

  /* Buttons */
  .btn {
    display: inline-block;
    width: 100%;
    padding: 1rem 2rem;
    font-size: 1rem;
    font-weight: 700;
    text-decoration: none;
    text-align: center;
    border-radius: 0;
    transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
    border: 2px solid transparent;
    min-height: 3rem;
    clip-path: polygon(12px 0, 100% 0, calc(100% - 12px) 100%, 0 100%);
  }

  .btn-primary {
    background: var(--black);
    color: var(--yellow);
    border-color: var(--black);
    box-shadow: 4px 4px 0 rgba(0, 0, 0, 0.2);
  }

  .btn-primary:hover {
    background: transparent;
    color: var(--black);
    border-color: var(--black);
    transform: translate(-2px, -2px);
    box-shadow: 6px 6px 0 rgba(0, 0, 0, 0.3);
  }

  .cta-dark .btn-primary {
    background: var(--yellow);
    color: var(--black);
  }

  .cta-dark .btn-primary:hover {
    background: transparent;
    color: var(--yellow);
    border-color: var(--yellow);
  }

  .btn-secondary {
    background: transparent;
    color: var(--white);
    border-color: var(--white);
    box-shadow: 4px 4px 0 rgba(255, 255, 255, 0.1);
  }

  .btn-secondary:hover {
    background: var(--white);
    color: var(--black);
    transform: translate(-2px, -2px);
    box-shadow: 6px 6px 0 rgba(255, 255, 255, 0.3);
  }

  .btn-large {
    padding: 1.5rem 3rem;
    font-size: 1.1rem;
  }

  /* CTA Section */
  .cta-section {
    background: linear-gradient(135deg, #000000 0%, #1a1a1a 100%);
    padding: 6rem 2rem;
    text-align: center;
    position: relative;
    overflow: hidden;
    margin-top: 4rem;
  }

  .cta-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 50% 50%, rgba(246, 245, 16, 0.1) 0%, transparent 60%);
  }

  .cta-content {
    position: relative;
    z-index: 1;
    max-width: 900px;
    margin: 0 auto;
  }

  .cta-section h2 {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 900;
    color: var(--white);
    margin-bottom: 1rem;
    letter-spacing: -1px;
    min-height: 2.5rem;
  }

  .cta-section p {
    font-size: 1.3rem;
    color: var(--gray-300);
    margin-bottom: 3rem;
    min-height: 1.5rem;
  }

  .cta-buttons {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .cta-section .btn-primary {
    background: var(--yellow);
    color: var(--black);
    border-color: var(--yellow);
  }

  .cta-section .btn-primary:hover {
    background: transparent;
    color: var(--yellow);
    border-color: var(--yellow);
    box-shadow: 6px 6px 0 rgba(246, 245, 16, 0.7), 0 0 25px rgba(246, 245, 16, 0.5);
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .content-layout {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .sidebar-column {
      position: relative;
      top: 0;
    }

    .features-grid {
      grid-template-columns: 1fr;
    }

    .neighborhood-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 768px) {
    .container {
      padding: 0 1.5rem;
    }

    .hero-content-wrapper {
      padding: 0 1.5rem;
    }

    .location-content-section {
      padding: 3rem 0;
    }

    .intro-section {
      margin-bottom: 3rem;
    }

    .location-card {
      padding: 2rem;
    }

    .features-grid,
    .neighborhoods-section {
      margin-bottom: 3rem;
    }

    .neighborhoods-section {
      padding: 2.5rem 1.5rem;
    }

    .faq-section {
      margin-top: 3rem;
      padding-top: 2rem;
    }
  }
</style>
